# Synthetic DBF corpus

Tiny deterministic files built with `tests.support.dbf.make_dbf`; no real patient records.
The manifest identifies content, encoding and initial Rust support.
Tests in Python and Rust consume these same bytes.
