# cnes_st

Schema source: `src/omnisus_db/data/dicionarios/cnes_st.yaml`

For the full field list, foreign keys, and decode tables, see the YAML
file in the repository. A future docs build will auto-render this page
from Frictionless metadata.
